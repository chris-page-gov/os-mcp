# Skills

This folder can contain Agent Skills in the https://agentskills.io format.

VS Code auto-discovers skills from `.github/skills/` (and `.claude/skills/` for backwards compatibility). Use those locations for best VS Code compatibility.

- Each skill is a directory with a `SKILL.md` file.
- See `docs/agent_skills.md` for authoring guidance.
